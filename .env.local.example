# Resend API key for contact form (/api/send) and sign-up emails (/api/signup)
RESEND_API_KEY=

# Verified sender address in Resend (e.g. contact@alvinchiew.com)
FROM_EMAIL=

# Phase B — activation key validator (optional until validator is deployed)
# ACTIVATION_ISSUE_URL=https://your-validator.example.com/api/v1/issue
# ACTIVATION_ISSUE_API_KEY=
